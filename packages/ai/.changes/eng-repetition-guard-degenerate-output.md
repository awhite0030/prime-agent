- Fixed a degenerate, non-progressing thinking/reasoning stream (e.g. a repeated-phrase loop) burning the whole output budget by aborting it early as a `degenerate_output` failure instead of streaming it to completion ([#1029](https://github.com/PrimeIntellect-ai/prime-agent/issues/1029), design, calibration, and tests originally by [@r0h1tb](https://github.com/r0h1tb) in #1099). Also wired `openai-completions` into the shared stream-failure classification used by every other provider, closing a separate gap where its failures had no classification at all. Kill switch: `PRIME_AGENT_NO_REPETITION_GUARD=1`.
