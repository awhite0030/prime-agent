// Ah, `buildSessionList` might rebuild all.
// What about "4. Kernel state snapshot re-serializes the whole namespace every turn."
// "5. RLM child accounting retains full sessions + kernels."
// Let's check `buildSessionList`.
